

import java.lang.management.ManagementFactory;
import java.text.DecimalFormat;
import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;


public class ThreadHavuzu {
 
  /*   static ExecutorService executor = Executors.newFixedThreadPool(10);  
    static long nanoSaniye;
    public static void main(String[] args) {
        threadSayisi();
    }
    static long nanoSaniye2;
    public static long threadSayisi() {
        benzerlikfiltre benzerlik = new benzerlikfiltre();
        for (int i = 0; i < benzerlik.threadsayisinigetir(); i++) {
            Runnable worker = new WorkerThread();
            executor.execute(worker);  
            nanoSaniye = ManagementFactory.getThreadMXBean().getThreadCpuTime(Thread.currentThread().getId());
            nanoSaniye2 += nanoSaniye;
        }
        executor.shutdown();
        while (!executor.isTerminated()) {
        }
        double sure = nanoSaniye * 1.66666667 * 0.00000000001;
        double sure2 = nanoSaniye2 * 1.66666667 * 0.00000000001;
        System.out.println("Finished all threads " + " " + "adet thread kullanildi" + "sure:" + new DecimalFormat("##.##").format(sure) + "dk");
        System.out.println("toplam sure:" + new DecimalFormat("##.##").format(sure2) + "dk");
        String sure21 = new DecimalFormat("##.##").format(sure2);
        Long varLong = Long.parseLong(sure21);
        return varLong;
    }*/
}
    
    
    
    
    
    
    

